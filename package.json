{
  "name": "aegis-command-center",
  "version": "8.0.0",
  "private": true,
  "description": "SB101 AEGIS v8 Autopilot — scheduled research, freshness gates, market history, card locking and server-side grading",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check src/engine.js && node --check src/store.js && node --check src/autopilot.js && node --check public/app.js && npm test"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
