name: NFL Shadow Scheduler Registration

on:
  schedule:
    - cron: "23 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nfl-shadow-staging:
    uses: jacobemrich-eng/aegis-sports-command-center/.github/workflows/nfl-shadow-validation.yml@nfl-shadow-staging
    with:
      scheduled: true
      publish: true
      mode: all
    secrets: inherit
